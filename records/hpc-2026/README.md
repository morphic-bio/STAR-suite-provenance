# Public HPC validation evidence

These records preserve the portable results used by the Bridges-2, Lustre, and
CPU-binding recipes: standalone finalizer memory reduction, placement effects,
and exact component-parallel timing with parity evidence.

They intentionally omit accounts, hosts, storage paths, job identifiers, and
all details of any non-public partition producer. The public recipe boundary is
an already validated partition manifest.
